# Todo App (CLI, JSON-backed)

A persistent command-line to-do list. Tasks are saved to `tasks.json`.

## Run
```bash
python main.py
```

## Commands
- `add <task>` — add a new task
- `list` — show all tasks
- `done <index>` — mark a task complete
- `remove <index>` — delete a task
- `exit` — quit
