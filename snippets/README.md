# Code Snippets

Small, reusable, copy-paste-ready code — not full examples, just handy utilities.

## Index
- `read_json.py` — load JSON safely
- `flatten_list.py` — flatten nested lists
- `timer_context.py` — context manager for timing a block of code

Add new snippets here as you find yourself reusing the same code across projects.
